import { Head, useForm } from '@inertiajs/react'; import HRLayout from '@/Layouts/HRLayout';
export default function Show({regularization:r}){const f=useForm({});return <HRLayout heading={`Regularization ${r.reference_no}`} subheading="Approval and audit trail"><Head title={r.reference_no}/><div className="max-w-2xl space-y-3 rounded-2xl border bg-white p-6"><p><b>Date:</b> {r.attendance_date}</p><p><b>Type:</b> {r.regularization_type}</p><p><b>Status:</b> {r.status}</p><p><b>Reason:</b> {r.reason}</p>{r.status==='draft'&&<button onClick={()=>f.post(route('hr.attendance.regularizations.submit',r.id))} className="rounded-xl bg-slate-900 px-4 py-2 text-white">Submit for Approval</button>}</div></HRLayout>;}
